@model IEnumerable<Product>




    @foreach (var item in Model)
{
    string imagePath = item.Images.FirstOrDefault(i => i.IsMain == true)?.Name;

    <tr>
        <td class="product-remove">
            <a onclick="removeFromFav(@item.Id,event)">
                <i class="fa fa-times" aria-hidden="true"></i>
            </a>
        </td>
        <td class="product-thumbnail">
            <a href="#">
                <img id="img-ddd" src="~/uploads/images/@imagePath" alt="cart-image" />
            </a>
        </td>
        <td class="product-name">
            <a asp-controller="shop"
           asp-action="details"
           asp-route-id="@item.Id">
                @item.Name
            </a>
        </td>
        <td class="product-price"><span class="amount">@item.Price</span></td>
        <td class="product-stock-status"><span>in stock</span></td>
        <td class="product-add-to-cart">
            <a onclick="addToBasket(event)" data-product-id="@item.Id"><i class="fa-solid fa-cart-plus"></i></a>
        </td>
    </tr>
}

    @*@foreach (var item in Model)
{
    string imagePath = item.Images.FirstOrDefault(i => i.IsMain == true)?.Name;
    <tr>
        <td width="45%">
            <div class="display-flex align-center">
                <div class="img-product">
                    <img width="40%" src="~/uploads/images/@imagePath" alt="" class="mCS_img_loaded">
                </div>
                <div class="name-product">
                    <a asp-controller="shop" asp-action="details" asp-route-id="@item.Id" class="pr-name">@item.Name</a>
                </div>
            </div>
        </td>
        <td width="15%" class="price">@item.Price</td>
        <td width="15%"><span class="in-stock-box">In Stock</span></td>
        <td width="15%"><button  onclick="addToBasket(event)" data-product-id="@item.Id" >Add to Cart</button></td>
        <td width="10%" class="text-center">
            
                <a class="product-remove-x" onclick="removeFromFav(@item.Id,event)">
                    <i class="fa fa-times"></i>
                </a>
           
        </td>
    </tr>
 
    
}*@
